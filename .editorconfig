root = true

[*]
indent_style = space
indent_size = 2
insert_final_newline = true
trim_trailing_whitespace = true

# Shell scripts must retain Unix line endings when edited on Windows.
[*.sh]
end_of_line = lf

# Cargo owns the layout of its generated lockfile.
[Cargo.lock]
indent_size = unset
